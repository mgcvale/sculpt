- Add unit tests
- Add Integration & memory leak tests
- Fix HTTP header parsing bugs & make parsing more robust
- Add more sc_easy_* helpers
- Add configurability through a config file instead of global constants in SCULPT.H
- Add configurability for HTTP Header parsing (parse with larger blocks for speed, even if that eats into the http body a bit)
